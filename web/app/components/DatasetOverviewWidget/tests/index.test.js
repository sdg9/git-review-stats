// import React from 'react';
// import { shallow } from 'enzyme';

// import DatasetOverviewWidget from '../index';

describe('<DatasetOverviewWidget />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
